{
  "config": {
    "step": {
      "user": {
        "title": "LumiLink Pool Light",
        "description": "Select a nearby LumiLink device or enter its Bluetooth address manually.",
        "data": {
          "address": "Bluetooth Address"
        }
      },
      "bluetooth_confirm": {
        "title": "LumiLink found",
        "description": "Do you want to add **{name}** to Home Assistant?"
      }
    },
    "error": {
      "no_devices_found": "No LumiLink devices found nearby. Enter the Bluetooth address manually."
    },
    "abort": {
      "already_configured": "This LumiLink device is already configured.",
      "no_devices_found": "No LumiLink devices could be found."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "LumiLink options",
        "description": "Lamp-sync behaviour. Sync mode anchors every colour selection with a reset (both lamps land on white together) and then steps slowly to the target colour, so lamps wired in parallel stay in sync.",
        "data": {
          "sync_mode": "Sync mode (reset before every colour change)",
          "step_delay": "Pause between colour steps (seconds)"
        }
      }
    }
  },
  "entity": {
    "button": {
      "sync_lamps": {
        "name": "Sync lamps"
      }
    }
  }
}
